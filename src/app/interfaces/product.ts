export interface Product {
  id: string;
  name: string;
  price: number;
  taxRate: number;
  stockSize: number;
  reorderLevel: number;
  description: string;
  creationDate: string;
  modifiedDate: string;
}
